# API Configuration

server:
  host: 0.0.0.0
  port: 8000

model:
  path: experiments/main/model/tft-main.ckpt
  prediction_length: 24
  use_bias_correction: true

cors:
  allow_origins:
    - "*"
  allow_methods:
    - "*"
  allow_headers:
    - "*"
